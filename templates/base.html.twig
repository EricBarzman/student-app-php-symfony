<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Student App{% endblock %}</title>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="/build/app.css">
        {% endblock %}
    </head>

    <body>
        <header class="container-fluid mb-4 bg-primary">
            <div class="d-flex justify-content-between align-items-center p-4">
                <a href="/">
                    <img src="/MuddyMooseWhite.png" style="width: 100px;" alt="logo"/>
                </a>
                <div class="d-flex align-self-center"> 
                    {% if app.user %}
                        <div class="btn-group">
                            <div class="p-2 border text-white">{{ app.user.username }}</div>
                            <a class="p-2 btn btn-info text-white" href="{{ path('app_logout') }}">Log out</a>
                        </div>{% endif %}
                </div>
        </header>

        {% block main %}{% endblock %}

        <footer class="text-end mt-5 p-3">
            <div>
                <p>2024 made by Eric Barzman</p>
            </div>
        </footer>
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        {% block additionalJS %}{% endblock %}
    </body>
</html>
